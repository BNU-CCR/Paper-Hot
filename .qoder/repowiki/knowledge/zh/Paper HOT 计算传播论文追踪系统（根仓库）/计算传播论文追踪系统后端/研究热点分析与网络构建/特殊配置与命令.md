需要设置 `ANTHROPIC_API_KEY`（可选 `anthropic_base_url` 和 `claude_model`）；嵌入模型权重缓存在项目根 `.cache/fastembed`；输出通过 `_ensure_output_dirs` + `_atomic_replace` 原子替换 `public_data_dir/hotspots/`，临时目录为 `.hotspots-next`。