通过`py -m journal_tracker.main <subcommand>`运行，常用子命令包括：`search`（仅搜索）、`screen-pending`（筛选待处理论文）、`workflow-status`（查看状态）、`export-public`（导出公开JSON）、`weekly-run`（完整周度流程，含--limit-per-journal/--screen-limit/--max-screen-batches/--refilter-limit/--skip-coverage参数）。