敏感凭据通过项目根目录下的 `.env`、`key.env` 或 `.local/key.env` 注入，也可通过环境变量 `ANTHROPIC_API_KEY`、`OPENALEX_API_KEY`、`SEMANTIC_SCHOLAR_API_KEY` 覆盖；数据库默认位于 `data/papers.db`（相对于 backend 根目录），公开导出目录为 `frontend/public/data` 或 `public/data`。