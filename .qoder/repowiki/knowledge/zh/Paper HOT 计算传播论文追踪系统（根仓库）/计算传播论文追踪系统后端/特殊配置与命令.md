通过 `py -m journal_tracker.main <subcommand>` 调用，常用子命令包括 search、export-public、generate-hotspots、backfill-bibliography、backfill-features、doctor、publish/unpublish、publish-high、list-public、update-public、workflow-status、weekly-run、build-hotspot-network、validate-hotspot-data；API Key 通过项目根目录 .env/key.env/.local/key.env 或环境变量注入。