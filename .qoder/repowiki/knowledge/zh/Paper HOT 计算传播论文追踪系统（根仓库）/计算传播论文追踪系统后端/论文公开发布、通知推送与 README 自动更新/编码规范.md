- 对外暴露的导出/更新功能均以独立类或纯函数形式组织，构造函数或入口函数从 `config.get_config()` 集中读取配置，避免硬编码。
- JSON 数据读写统一使用 `pathlib.Path` 的 `read_text`/`write_text` 并显式指定 `encoding="utf-8"`，输出目录通过 `parent.mkdir(parents=True, exist_ok=True)` 确保存在。
- README 自动化更新采用前后端约定的 HTML 注释标记（`paper-hot:auto-preview:*`、`paper-hot:auto-stats:*`）作为插入锚点，通过 `replace_section` 精确替换区块内容。
- 通知发送方法统一返回 `bool` 表示成功与否，并在失败时通过 `print` 打印日志而非抛出异常，保证上层调用链稳定。
- 字符串处理使用辅助私有方法（如 `_split_csv_field`、`_slugify`、`_escape_markdown`、`_paper_link`）封装通用逻辑，保持主流程简洁。