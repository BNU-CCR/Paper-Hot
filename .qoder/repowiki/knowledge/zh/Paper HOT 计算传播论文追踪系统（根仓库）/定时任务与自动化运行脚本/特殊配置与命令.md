`register_weekly_task.ps1` 需在以管理员权限运行的 PowerShell 中执行，支持 `-DayOfWeek`、`-At`、`-PushPublicData`、`-SkipCoverage` 参数；`run_weekly.ps1` 和 `run_tracker.sh` 可直接运行，前者在 Windows 上通过计划任务触发，后者在 Linux/macOS 上由系统 cron 或其他调度器调用。