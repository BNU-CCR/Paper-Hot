React 函数组件 + TypeScript；UI 原语来自 `@radix-ui/react-*`（select/dialog/tabs/collapsible/slot）；样式通过 Tailwind CSS 原子类与 `class-variance-authority` 的 cva 系统管理；图标使用 `lucide-react`；className 合并统一走 `../../lib/utils` 的 `cn` 函数。