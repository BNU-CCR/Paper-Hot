- 所有脚本命令统一通过 npm scripts 暴露（dev/build/start/typecheck），不直接调用 CLI 工具。
- TypeScript 配置启用 strict、isolatedModules、noEmit，由 Next.js 插件接管 JSX 与增量编译。
- 路径别名 `@/*` 映射到项目根目录，组件与模块导入统一使用该前缀。
- 构建产物为静态导出，图片默认未优化，URL 统一保留尾部斜杠以兼容静态托管。